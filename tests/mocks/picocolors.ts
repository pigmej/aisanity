export const green = (str: string) => str;
export const blue = (str: string) => str;
export const yellow = (str: string) => str;
export const red = (str: string) => str;
export default {
  green,
  blue,
  yellow,
  red
};